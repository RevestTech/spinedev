# Tron - Enterprise AI QA & Development Platform
# Python 3.11+ required

# ── Core Framework ──
fastapi==0.124.4
uvicorn[standard]==0.32.1
pydantic==2.10.6
pydantic-settings==2.7.1

# ── Database ──
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
pgvector==0.2.3
psycopg2-binary==2.9.9
alembic==1.13.1

# ── Redis & Cache ──
redis==5.0.1
hiredis==2.2.3

# ── Workflow Engine ──
temporalio==1.4.0

# ── AI/ML ──
openai==1.59.9
anthropic==0.42.0
tiktoken==0.5.2

# ── Object Storage ──
minio==7.2.0

# ── Secrets Management ──
hvac==2.1.0

# ── WebSocket ──
python-socketio==5.10.0
aioredis==2.0.1

# ── Monitoring & Observability ──
prometheus-client==0.19.0
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-redis==0.48b0
opentelemetry-instrumentation-httpx==0.48b0

# ── Auth & Security ──
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# ── HTTP & Resilience ──
httpx==0.27.2
tenacity==8.2.3
pybreaker==1.0.2

# ── Sandbox & Isolation ──
# 7.1.0+ fixes http+docker with modern urllib3/requests (7.0.0 breaks sandbox).
docker==7.1.0

# ── Testing ──
aiosqlite==0.20.0
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
locust==2.19.1
playwright==1.40.0

# ── CLI (Typer; pin click — Typer 0.12 + Click 8.2+ breaks help rendering) ──
typer==0.12.5
click==8.1.7

# ── MCP stdio server (same venv as API: python -m tron.mcp) ──
mcp[cli]==1.2.0

# ── Code Quality ──
ruff==0.1.7
mypy==1.7.1
bandit==1.7.5
semgrep==1.52.0
