"""TRON-side unit tests (FIX1 onward — co-located with TRON code).

Pre-existing TRON tests still live at ``verify/tests/`` (subtree-merged
from upstream TRON). This directory holds Spine-authored tests for the
Spine ↔ TRON bridge layer (``verify/tron/infra/llm/client.py`` shim,
etc.) so they don't conflict with TRON subtree pulls.
"""
