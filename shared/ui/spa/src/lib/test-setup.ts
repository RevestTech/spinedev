// Spine Hub SPA — vitest setup (V3 Wave 3 part 2, Squad SPA1).
// Pulled in by vite.config.ts:test.setupFiles. Adds the
// @testing-library/jest-dom matchers so tests can use toBeInTheDocument etc.
import '@testing-library/jest-dom/vitest';
