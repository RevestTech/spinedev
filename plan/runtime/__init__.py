"""Plan-subsystem runtime helpers (intake loop, dispatch glue).

Kept separate from `plan/artifacts/` (schemas) and `plan/pipeline/` (manifest
loader) so the intake / future LLM-driven flows have a clear home.
"""
